{
  "name": "gotlike-benchmark",
  "version": "1.0.0",
  "private": true,
  "description": "Benchmarks gotlike against got, undici and undici fetch",
  "license": "ISC",
  "author": "Ivan Kotlaja",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "bench": "npm --prefix .. run build && node index.ts",
    "typecheck": "npm --prefix .. run build && ../node_modules/.bin/tsc -p tsconfig.json"
  },
  "dependencies": {
    "got": "^14.4.1"
  }
}
